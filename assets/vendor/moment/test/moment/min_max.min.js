var moment=require("../../moment");exports.minMax={setUp:function(a){moment.locale("en");moment.createFromInputFallback=function(){throw new Error("input not handled by moment")};a()},tearDown:function(a){moment.locale("en");a()},min:function(d){var b=moment(),a=b.clone().add(1,"month"),c=b.clone().subtract(1,"month");d.equal(moment.min(b,a,c),c,"min(now, future, past)");d.equal(moment.min(a,b,c),c,"min(future, now, past)");d.equal(moment.min(a,c,b),c,"min(future, past, now)");d.equal(moment.min(c,a,b),c,"min(past, future, now)");d.equal(moment.min(b,c),c,"min(now, past)");d.equal(moment.min(c,b),c,"min(past, now)");d.equal(moment.min(b),b,"min(now, past)");d.equal(moment.min([b,a,c]),c,"min([now, future, past])");d.equal(moment.min([b,c]),c,"min(now, past)");d.equal(moment.min([b]),b,"min(now)");d.done()},max:function(d){var b=moment(),a=b.clone().add(1,"month"),c=b.clone().subtract(1,"month");d.equal(moment.max(b,a,c),a,"max(now, future, past)");d.equal(moment.max(a,b,c),a,"max(future, now, past)");d.equal(moment.max(a,c,b),a,"max(future, past, now)");d.equal(moment.max(c,a,b),a,"max(past, future, now)");d.equal(moment.max(b,c),b,"max(now, past)");d.equal(moment.max(c,b),b,"max(past, now)");d.equal(moment.max(b),b,"max(now, past)");d.equal(moment.max([b,a,c]),a,"max([now, future, past])");d.equal(moment.max([b,c]),b,"max(now, past)");d.equal(moment.max([b]),b,"max(now)");d.done()}};