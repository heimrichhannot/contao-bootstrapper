var moment=require("../../moment");exports.relativeTime={"default thresholds":function(c){var b=moment();b.subtract(44,"seconds");c.equal(b.fromNow(),"a few seconds ago","Below default seconds to minutes threshold");b.subtract(1,"seconds");c.equal(b.fromNow(),"a minute ago","Above default seconds to minutes threshold");b=moment();b.subtract(44,"minutes");c.equal(b.fromNow(),"44 minutes ago","Below default minute to hour threshold");b.subtract(1,"minutes");c.equal(b.fromNow(),"an hour ago","Above default minute to hour threshold");b=moment();b.subtract(21,"hours");c.equal(b.fromNow(),"21 hours ago","Below default hours to day threshold");b.subtract(1,"hours");c.equal(b.fromNow(),"a day ago","Above default hours to day threshold");b=moment();b.subtract(25,"days");c.equal(b.fromNow(),"25 days ago","Below default days to month (singular) threshold");b.subtract(1,"days");c.equal(b.fromNow(),"a month ago","Above default days to month (singular) threshold");b=moment();b.subtract(10,"months");c.equal(b.fromNow(),"10 months ago","Below default days to years threshold");b.subtract(1,"month");c.equal(b.fromNow(),"a year ago","Above default days to years threshold");c.done()},"custom thresholds":function(c){moment.relativeTimeThreshold("s",55);var b=moment();b.subtract(54,"seconds");c.equal(b.fromNow(),"a few seconds ago","Below custom seconds to minutes threshold");b.subtract(1,"seconds");c.equal(b.fromNow(),"a minute ago","Above custom seconds to minutes threshold");moment.relativeTimeThreshold("s",45);moment.relativeTimeThreshold("m",55);b=moment();b.subtract(54,"minutes");c.equal(b.fromNow(),"54 minutes ago","Below custom minutes to hours threshold");b.subtract(1,"minutes");c.equal(b.fromNow(),"an hour ago","Above custom minutes to hours threshold");moment.relativeTimeThreshold("m",45);moment.relativeTimeThreshold("h",24);b=moment();b.subtract(23,"hours");c.equal(b.fromNow(),"23 hours ago","Below custom hours to days threshold");b.subtract(1,"hours");c.equal(b.fromNow(),"a day ago","Above custom hours to days threshold");moment.relativeTimeThreshold("h",22);moment.relativeTimeThreshold("d",28);b=moment();b.subtract(27,"days");c.equal(b.fromNow(),"27 days ago","Below custom days to month (singular) threshold");b.subtract(1,"days");c.equal(b.fromNow(),"a month ago","Above custom days to month (singular) threshold");moment.relativeTimeThreshold("d",26);moment.relativeTimeThreshold("M",9);b=moment();b.subtract(8,"months");c.equal(b.fromNow(),"8 months ago","Below custom days to years threshold");b.subtract(1,"months");c.equal(b.fromNow(),"a year ago","Above custom days to years threshold");moment.relativeTimeThreshold("M",11);c.done()},"retrive threshold settings":function(b){b.expect(1);moment.relativeTimeThreshold("m",45);var a=moment.relativeTimeThreshold("m");b.equal(a,45,"Can retrieve minute setting");b.done()}};